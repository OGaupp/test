# Copilot Instructions

## General Guidelines
- User prefers building against the newest available .NET/.NET SDK versions by default, unless there are technically significant blockers.
- User prefers targeting .NET 10 when possible.
- For this project, a standard executable desktop application (unpackaged EXE) should be used instead of a Windows Store/MSIX app, when technically feasible.